// Same loading UI as every other public page. The home page lives in its own
// route group (see layout.jsx) and so needs its own copy of this boundary.
export { default } from "../(public)/loading";
